#ifndef ARRAY_HELPER_H
#define VECTOR_HELPER_H

#include <iostream>
#include <vector>

using namespace std;

namespace ArrayHelper {
  bool isInArray(string* array, int length, string element);
};

#endif